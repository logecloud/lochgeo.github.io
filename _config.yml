# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
name: lochgeo's blog
title: lochgeo's blog
description: >- # this means to ignore newlines until "baseurl:"
  The code behind the magic
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://lochgeo.github.io" # the base hostname & protocol for your site, e.g. http://example.com
avatar: "/images/lg-logos_transparent.png"

# Author
author:
  name: lochgeo
  avatar: "/images/lg-logos_transparent.png"

# Jekyll settings
markdown: kramdown
highlighter: rouge
permalink: /:year/:month/:day/:title/

# Pagination settings
paginate: 5
paginate_path: "/page:num/"

# Build settings
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-paginate

# Kramdown settings
kramdown:
  input: GFM
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: 'highlight'

# Navigation
navigation:
  - title: "Home"
    url: "/"
  - title: "About"
    url: "/about/"
  - title: "Search"
    url: "/search/"

# Social links
github_username: lochgeo
twitter_username: lochgeo

# Google Analytics
google_analytics: G-W2NYSGMPTH

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - CNAME